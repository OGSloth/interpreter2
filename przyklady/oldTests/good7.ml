// for with reading

int main () {
   int x = 0 ;
   x = binom();
   if (x == 5 * 4 / 2) 
	print("Binom");
   return 0;
}

int binom() {
   int x = 0;
   for (i = 1 to 5) {
	x = x + i;
   }
   return x;
}
